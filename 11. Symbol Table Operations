#include <stdio.h>
#include <string.h>

struct sym {
    char name[10];
} s[10];

int main() {
    strcpy(s[0].name,"x");
    strcpy(s[1].name,"y");

    for(int i=0;i<2;i++)
        printf("%s\n",s[i].name);

    return 0;
}


x
y
