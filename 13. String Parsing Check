#include <stdio.h>
#include <string.h>

int main() {
    char s[20];
    scanf("%s",s);
    if(strlen(s)>0)
        printf("Accepted");
    else
        printf("Rejected");
    return 0;
}


Accepted
